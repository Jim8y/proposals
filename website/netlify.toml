[build]
  command = "bash netlify-build.sh"
  publish = "build"

[build.environment]
  NODE_VERSION = "18"

[dev]
  command = "npm run start"
  port = 8888
  targetPort = 3000
  publish = "build"
  autoLaunch = true

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200